export const enum ReplaceFlags {
	// get the content of the current note
	CONTENT = "content",
	// get all tags in the vault
	TAGS = "tags",
	// get the current date in the format specified if has format
	DATE = "date",
	// get the selected text
	SELECTED = "selected"
}

